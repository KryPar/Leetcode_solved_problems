class Solution:
    def isPalindrome(self, x: int) -> bool:
        if x >= 0:
            y = list(str(x))
            y.reverse()

            f = "".join(y)
            z = int(f)

            return x == z

        else:
            return False
