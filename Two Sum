class Solution:
     def twoSum(self, nums: list[int], target: int) -> list[int]:
        for idx in range(len(nums)):
            for idex in range(idx + 1, len(nums)):
                if nums[idx] + nums[idex] == target:
                    return [idx, idex]
        return []
